# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.
base: &base
  spotify_client_id: <%= ENV['SPOTIFY_CLIENT_ID'] %>
  spotify_secret: <%= ENV['SPOTIFY_SECRET'] %>
  slack_secret: <%= ENV['SLACK_SECRET'] %>
  slack_verification_token: <%= ENV['SLACK_V_TOKEN'] %>

development:
  <<: *base
  secret_key_base: 4f6849c3d11cbb1e1f65d7e12d6e993268ef6e9b6e9ef3f051bc1f04e5e80a996176d8d5d0cb427ef1bf1aea45d5b5d98e2f311ed0eb99fa6f6a9c7d038b96f2

test:
  <<: *base
  secret_key_base: e3c33c1968537f25579ffff3edcded9165d0cd54a149724213ba32f7116de40382f092c0dfd7b8ffc178e85d70d801e3b93dc71d801aef5feb24444525cb5274

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  <<: *base
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
