language: ruby
cache: bundler
sudo: false
before_install:
  - gem install bundler
  - gem update --system
  - gem --version
before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
rvm:
  - 2.3.3
services:
  - postgresql
